// this code applies for register and login

const inputs = document.querySelectorAll(".register-inputs");

inputs && inputs.forEach(item => item.addEventListener("keydown", e => item.style.border = "none"));